n, t = map(int, input().split())
arr = list(map(int, input().split()))

left = 0
total = 0
ans = 0

for right in range(n):
    total += arr[right]

    while total > t:
        total -= arr[left]
        left += 1

    ans = max(ans, right - left + 1)

print(ans)
